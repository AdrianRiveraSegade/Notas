const newNote = require("./newNote");
const getNote = require("./getNote");
const addNotePhoto = require("./addNotePhoto");
const deleteNotePhoto = require("./deleteNotePhoto");

module.exports = {
  newNote,
  getNote,
  addNotePhoto,
  deleteNotePhoto,
};
